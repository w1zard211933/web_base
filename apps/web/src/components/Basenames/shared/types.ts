export enum FlowBackgroundSteps {
  Search = 'search', // Floating ENS pills (registration only)
  Form = 'form', // Globe video with gray background (registration claim, renewal form)
  Pending = 'pending', // Vortex animation with gray background
  Success = 'success', // Fireworks video with blue background
}
